import 'package:flutter/material.dart';

class ThemeColor {
  static const primaryColor = Color(0xff212028);
  static const accentColor = Color(0xff1a1936);
  static const mainTextColor = Color(0xffffffff);
}
